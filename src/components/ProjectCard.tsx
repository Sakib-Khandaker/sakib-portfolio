import Link from "next/link";
import { ArrowUpRight } from "lucide-react";
export default function ProjectCard({ project }: { project: { title: string; category: string; description: string; tech: string[]; status: string; href: string } }) { return <article className="panel group p-6 transition hover:-translate-y-1 hover:border-brand/70 hover:shadow-glow"><div className="flex items-center justify-between gap-3"><span className="text-sm font-semibold text-cyan">{project.category}</span><span className="tag">{project.status}</span></div><h3 className="mt-5 text-xl font-semibold">{project.title}</h3><p className="mt-3 leading-7 text-slate-400">{project.description}</p><div className="mt-5 flex flex-wrap gap-2">{project.tech.map((item) => <span key={item} className="tag">{item}</span>)}</div><Link href={project.href} className="mt-6 inline-flex items-center gap-2 text-sm font-semibold text-blue-400 hover:text-cyan">View details <ArrowUpRight size={16}/></Link></article>; }
